# Tools to concatenate the individual photometry files from each tile into one hdf5 file

Also converts the ShapePipe catalog to hdf5 and does some matching between them if requested/neccesary

usage: 

`python concat.py --bands {ugriz or ugri} --config {config_file}`
